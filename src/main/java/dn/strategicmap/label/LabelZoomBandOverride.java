package dn.strategicmap.label;

/** Author-selected minimum zoom band, or the catalogue default. */
public enum LabelZoomBandOverride {
  DEFAULT,
  WORLD,
  GRAND,
  THEATRE,
  NATIONAL,
  REGIONAL,
  LOCAL,
  DETAIL
}
